import { createTheme } from '@mui/system'

const theme = createTheme({
  palette: {
    primary: {
      main: '#226cd6',
    },
    secondary: {
      main: '#3cb371',
    },
    background: {
      default: '#fff',
    },
  },
})

export default theme
